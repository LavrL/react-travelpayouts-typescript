import * as engVersion from '../locales/en';
import * as ruVersion from '../locales/ru';

const resources = {
  en: {
    translation: engVersion
  },
  ru: {
    translation: ruVersion
  }
};

export default resources;